"""Analyzer — walks projects and extracts structural metadata.

Pure deterministic AST analysis. No LLM, no embeddings, no judgment.
Returns structured data that the generator and the LLM can use.
"""
